'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = template;

var _render = require('./render');

var _render2 = _interopRequireDefault(_render);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 * template
 * Tagged template literal function
 * @param {string[]} strings
 * @param {any[]} keys
 * @returns {function}
 */
function template(strings) {
  for (var _len = arguments.length, keys = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    keys[_key - 1] = arguments[_key];
  }

  if (typeof strings === 'undefined') {
    throw new Error('You must use template with template literal');
  }

  try {
    var binding = Object.assign(this.binding, { strings: strings, keys: keys });
    return _render2.default.bind({ binding: binding });
  } catch (e) {
    throw new Error('You must bind template function if not using factory');
  }
}