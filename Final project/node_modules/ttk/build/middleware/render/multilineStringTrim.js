'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = multilineStringTrim;
/**
 * Trims multiline strings
 */
function multilineStringTrim(text) {
  /** Array of lines from trimmed text */
  var lines = text.trim().split("\n");
  /** Shortest count of whitespace at beginning of lines */
  var shortestLead = Math.min.apply(null, lines.map(function (l) {
    return l.search(/^\S/);
  }));
  /** String of n spaces to remove for normalization */
  var search = Array(shortestLead + 1).join(' ');
  /** Lines joined in to return string */
  var trimmedString = lines.map(function (l) {
    return l.replace(search, '');
  }).join("\n");
  return trimmedString;
}